# Copy to .env.local and adjust if needed. .env.local is gitignored.

# Base URL of the FastAPI backend. Defaults to localhost:8000 in dev.
# Use the full origin including http(s):// and port; no trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8000
